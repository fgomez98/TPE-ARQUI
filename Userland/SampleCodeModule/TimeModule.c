//
//  TimeModule.c
//  
//
//  Created by Fermin Gomez on 5/22/18.
//
//

#include <stdio.h>
// Donde van los modulos
// Definir HOTKEY
// Incluir los .h
// Cuando corta?? lo hace el shell
/*
void initializeHour(){
    int colours[5] = {};
    int colour = colours[0];
    int i = 0;
    
    while(true){
        char * currTime = getTime();
        if(keyPressed() && getKey() = HOTKEY){
            colour = colours[++i];
            i= i%6;
        }
        showTime(colour, currTime);
    }
}
*/